## 1. 后端修改 - 移除自动处理

- [ ] 1.1 修改 `backend/app/services/file.py`，移除第 127-129 行的 `enqueue` 调用
- [ ] 1.2 验证上传后状态保持为 `pending`

## 2. 后端 API - 新增批量处理

- [ ] 2.1 在 `backend/app/api/file.py` 新增 `POST /api/contents/batch-process` 端点
- [ ] 2.2 实现批量处理逻辑：接收 `content_ids` 数组，循环调用 `enqueue`
- [ ] 2.3 返回批量处理接收确认

## 3. 前端 API - 新增批量处理调用

- [ ] 3.1 在 `frontend/src/api/content.ts` 新增 `batchProcess` 方法
- [ ] 3.2 确保单个处理方法 `processContent` 可正常使用

## 4. 前端列表页 - 复选框选择

- [ ] 4.1 在内容列表页添加选中状态管理（useState 存储选中的 ID 数组）
- [ ] 4.2 在每个内容卡片上添加复选框
- [ ] 4.3 实现全选/取消全选功能
- [ ] 4.4 在顶部添加「批量处理」按钮（仅当有选中项时显示）

## 5. 前端列表页 - 处理按钮

- [ ] 5.1 在内容卡片上添加「处理」按钮（仅当状态为 `pending` 时显示）
- [ ] 5.2 点击处理按钮后，状态立即更新为 `processing`
- [ ] 5.3 处理按钮在处理中状态显示为加载/禁用状态

## 6. 前端详情页 - 处理按钮

- [ ] 6.1 确认详情页的「重新处理」按钮可正常工作
- [ ] 6.2 确保首次处理时也显示正确的状态

## 7. 测试验证

- [ ] 7.1 测试单文件上传：验证状态为 pending，不自动处理
- [ ] 7.2 测试单个内容处理：点击处理按钮后状态变为 processing
- [ ] 7.3 测试批量处理：选择多个内容后点击批量处理
- [ ] 7.4 测试处理完成：状态正确更新为 completed/failed
